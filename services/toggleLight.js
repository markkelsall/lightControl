export function (room, turnOn) {
  if (room !== undefined) {
    if (turnOn) {
      //hit url of light with true with room.lightUrl
    } else {
      //hit url of light with false with room.lightUrl
    }
  } else {
    console.log('no point running log if no room').
  }
}
